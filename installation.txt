Mint OS Already comes with the C, C++, Python3 and java pre-installed 

<-NODE and NPM->
Node as well comes pre-installed but with older version to upgrade 
>sudo apt remove nodejs npm -y
>sudo apt install curl -y
curl -L https://raw.githubusercontent.com/tj/n/master/bin/n -o n
sudo bash n lts # Just to make sure `n` itself can run
sudo mv n /usr/local/bin/
>npm install -g npm@latest

<-Vscode->
>sudo apt install wget gpg
wget -qO- https://packages.microsoft.com/keys/microsoft.asc | gpg --dearmor > packages.microsoft.gpg
sudo install -o root -g root -m 644 packages.microsoft.gpg /etc/apt/trusted.gpg.d/
sudo sh -c 'echo "deb [arch=amd64] https://packages.microsoft.com/repos/vscode stable main" > /etc/apt/sources.list.d/vscode.list'
sudo apt update
sudo apt install code

<-Git and Github->
>sudo apt install git -y
>git config --global user.name "YourUserName"
>git config --global user.email "UserEmailId"
>ssh-keygen -t ed25519 -C "UserEmailId"
#press Enter to accept default location , press Enter if don't want a passphrase
>eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
>cat ~/.ssh/id_ed25519.pub
you will see a public key with email attached
>ssh -T git@github.com
Press Enter Enter
>#cat ~/.ssh/id_ed25519
#-----BEGIN OPENSSH PRIVATE KEY-----
there will be SSH key showing up
-----END OPENSSH PRIVATE KEY-----


<-CONKY MANAGER 2 & neofetch & and all inside software manager->
>sudo install neofetch
#conky-manager-2, Chromium , Simplescreenrecorder  install it from software manager

<-Theme and Color->
https://www.cinnamon-look.org/p/1715554
get the theme whichever you want download and extract the file and move the main file which consist the gtk-4.0 move the file containing the folder like parent file of the gtk-4.0 into the ~/.theme and check for the
mint os -> system settings -> themes -> adv setting -> change the application and the deskop with the theme that you added 

